import { makeStyles } from '@material-ui/core';

export const useTodoAppPageStyles = makeStyles(() => ({
  todoContainer: {
    marginTop: '20px',
  },
  paperWrapper: {
    position: 'relative',
    padding: '20px',
  },
}));
