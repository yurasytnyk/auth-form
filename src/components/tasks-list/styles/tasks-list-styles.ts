import { makeStyles } from '@material-ui/core';

export const useTasksListStyles = makeStyles(() => ({
  tasksList: {
    margin: 0,
    marginTop: '20px',
    padding: 0,
    listStyleType: 'none',
  },
}));
