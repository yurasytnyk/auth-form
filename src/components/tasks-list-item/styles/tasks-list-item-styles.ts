import { makeStyles } from '@material-ui/core';

export const useTasksListItemStyles = makeStyles(() => ({
  tasksListItem: {
    display: 'flex',
    justifyContent: 'space-between',
    flex: '1 1 100%',
  },
}));
